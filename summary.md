## Lab1 :MapReduce
## Lab 2：KV Server 总结
这个实验在已经了解分布式系统基础概念的前提下，整体不算难，主要难点在锁的实现上，细节比较多，也容易出错。
实验的第一部分是实现一个**基于版本号的 KV 存储**。
其中：
* `server` 负责保存键值对和版本号
* `client` 负责封装操作逻辑并通过 RPC 与 `server` 通信
之所以使用 RPC，是因为在实际场景中，服务端通常不会只部署在单机上。
在完成 KV 存储后，实验要求基于它实现一个**锁**，逻辑上类似于 Redis 分布式锁。这里锁的释放不是删除 key，而是将对应的 `value` 设为 `""`，表示锁空闲。
在此基础上，实验进一步加入了**不可靠网络**这一因素，因此需要为客户端实现**重试机制**。这一部分最关键的问题是：客户端有时无法判断一次 `Put` 是否真正执行成功。
典型情况有两种：
1. Client A 调用 `Put` 后实际上已经成功加锁，但服务端回复丢失，A 重试后收到 `ErrVersion`。此时锁其实已经被 A 持有。
2. Client A 调用 `Put` 期间，Client B 先一步完成了更新，导致 A 的请求返回 `ErrVersion`。此时 A 的操作实际上没有成功。
对于客户端来说，这两种情况无法区分，因此不能简单把它们都当成失败处理。实验中的做法是将这类不确定结果统一封装为 `ErrMaybe` 返回给锁层，再由锁逻辑结合当前状态继续判断和处理。

